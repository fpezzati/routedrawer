language: node_js
node_js:
 - "node"
sudo: required
services:
  - docker

before-install:
  - docker build -t fpezzati/routedrawingbook -f ./routedrawingbook/Dockerfile.dev .

scripts:
  - docker run fpezzati/routedrawingbook npm run test
#  - docker run -v $(pwd)/routedrawingbook:/home/app fpezzati/routedrawingbook npm run test
